# Clave gratuita del alta DIRECTA (sin RapidAPI) en https://dashboard.api-football.com/register
API_FOOTBALL_KEY=tu_api_key_aqui

# Puerto donde corre el servidor (backend + frontend estático)
PORT=3001
